"""Shared AI prompt fragments used across all modules."""

LANGUAGE_INSTRUCTION = (
    "Always respond in the same language as the user's input. "
    "If the language is ambiguous or cannot be determined, default to English."
)
